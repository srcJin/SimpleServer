<!DOCTYPE html>
<html>
  <head><title>Login</title></head>
  <body>
    <form method="POST">
      {{{form}}}

      <div>
        <div>
          {{#each errors}}
          <p>{{this}}</p>
          {{/each}}
        </div>

        <input type="submit" value="Login"/>
      </div>
    </form>
  </body>
</html>